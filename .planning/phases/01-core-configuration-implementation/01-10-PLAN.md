---
phase: 01-core-configuration-implementation
plan: 10
type: execute
wave: 3
depends_on: ["01-08", "01-09"]
files_modified:
  - src/config/integration.test.ts
  - config/providers.yaml
autonomous: true
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "End-to-end config flow validated by integration test"
    - "Actual config/providers.yaml loads successfully"
    - "All providers from config are available at runtime"
  artifacts:
    - path: "src/config/integration.test.ts"
      provides: "End-to-end config validation"
      min_lines: 200
    - path: "config/providers.yaml"
      provides: "Production provider configuration"
      min_lines: 200
  key_links:
    - from: "integration test"
      to: "config/providers.yaml"
      via: "loadConfigFile call"
      pattern: "loadConfigFile.*providers\\.yaml"
    - from: "integration test"
      to: "src/config/index.ts"
      via: "initializeConfig call"
      pattern: "initializeConfig.*getEnabledProviders"
---

<objective>
Add comprehensive integration test validating end-to-end configuration flow from file to runtime providers.

Purpose: While unit tests validate individual components, there's no test validating the complete flow: loading config/providers.yaml, parsing it, validating schema, applying ENV overrides, and making providers available via getEnabledProviders(). This plan adds that end-to-end validation.

Output: Integration test proves that config/providers.yaml loads correctly and all 9 providers are available at runtime with proper API key merging.
</objective>

<execution_context>
@/Users/antonioreid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonioreid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-configuration-implementation/01-core-configuration-implementation-VERIFICATION.md
@.planning/phases/01-core-configuration-implementation/01-08-SUMMARY.md
@.planning/phases/01-core-configuration-implementation/01-09-SUMMARY.md
@src/config/integration.test.ts
@config/providers.yaml
</context>

<tasks>

<task type="auto" priority="high" estimate="30m">
  <name>Add end-to-end config loading test</name>
  <files>src/config/integration.test.ts</files>
  <action>
    Add a comprehensive integration test that validates the complete config loading flow:

    1. Create new test suite "End-to-End Config Loading":

    2. Test "should load real config/providers.yaml and make providers available":
       - Set PROVIDER_MISTRAL_API_KEY, PROVIDER_GROQ_API_KEY, PROVIDER_GEMINI_API_KEY
       - Call initializeConfig() without arguments (uses default config file)
       - Call getConfig() and verify config structure
       - Call getEnabledProviders() and verify providers with API keys are present
       - Verify all 9 providers from config/providers.yaml are loaded

    3. Test "should filter out providers without API keys":
       - Set only PROVIDER_MISTRAL_API_KEY
       - Call initializeConfig()
       - Verify getEnabledProviders() returns only mistral
       - Verify groq, gemini, etc. are not in enabled list

    4. Test "should merge API keys from ENV into provider objects":
       - Set PROVIDER_MISTRAL_API_KEY to test value
       - Call initializeConfig()
       - Get enabled providers
       - Verify mistral provider has correct apiKey merged from ENV
       - Verify apiKey matches PROVIDER_MISTRAL_API_KEY value

    5. Test "should load provider models from config file":
       - Set at least one provider API key
       - Call initializeConfig()
       - Get enabled providers
       - Verify each provider has correct models array from config
       - Verify model properties (id, name, contextWindow, maxOutput, capabilities, quota, tier)

    This test should NOT mock anything - it should test the real loader, validator, and config initialization.
  </action>
  <verify>npm test -- src/config/integration.test.ts</verify>
  <done>
    - Integration test loads real config/providers.yaml
    - Test validates all 9 providers are present
    - Test confirms API key merging works correctly
    - Test verifies provider filtering based on API keys
    - Test validates model definitions from config file
    - All integration tests pass
  </done>
  <commit_message>test(01-10): add end-to-end config loading integration test

- Add test for loading real config/providers.yaml
- Validate all 9 providers are present in config
- Confirm API key merging from ENV works correctly
- Verify provider filtering based on API key presence
- Validate model definitions loaded from config file
</commit_message>
</task>

<task type="auto" priority="medium" estimate="20m">
  <name>Add environment override validation test</name>
  <files>src/config/integration.test.ts</files>
  <action>
    Add test validating environment variable override behavior:

    1. Create test "should override provider enabled status via ENV":
       - Load config where groq.enabled: false in config/providers.yaml
       - Set PROVIDER_GROQ_ENABLED=true
       - Set PROVIDER_GROQ_API_KEY
       - Call initializeConfig()
       - Verify groq appears in getEnabledProviders() despite being disabled in file

    2. Create test "should use ENV API key over config file API key":
       - If config file has providerConfig.groq.apiKey: 'file-key'
       - Set PROVIDER_GROQ_API_KEY='env-key'
       - Call initializeConfig()
       - Verify groq provider uses 'env-key' not 'file-key'
       - Note: Current implementation may not support API keys in config file, document actual behavior

    3. Create test "should handle missing ENV variables gracefully":
       - Set no provider API keys
       - Call initializeConfig()
       - Verify getEnabledProviders() returns empty array
       - Verify no errors thrown, just empty provider list

    These tests validate the ENV override system documented in env-override.ts.
  </action>
  <verify>npm test -- src/config/integration.test.ts</verify>
  <done>
    - Test validates PROVIDER_{ID}_ENABLED override works
    - Test validates PROVIDER_{ID}_API_KEY override works
    - Test confirms ENV takes precedence over config file
    - Test handles missing ENV variables gracefully
    - All override tests pass
  </done>
  <commit_message>test(01-10): add environment override validation tests

- Test PROVIDER_{ID}_ENABLED override behavior
- Test PROVIDER_{ID}_API_KEY override precedence
- Validate ENV variables override config file values
- Test graceful handling of missing ENV variables
</commit_message>
</task>

<task type="auto" priority="medium" estimate="15m">
  <name>Add config validation error test</name>
  <files>src/config/integration.test.ts</files>
  <action>
    Add test validating config validation error handling:

    1. Create test "should reject invalid config with clear error":
       - Create temp config file with invalid data (e.g., invalid URL, missing required field)
       - Call initializeConfig() with this file
       - Verify error is thrown
       - Verify error message is descriptive
       - Verify error mentions what's invalid

    2. Create test "should provide helpful error when config file missing":
       - Call initializeConfig() with non-existent config file
       - Verify error is thrown
       - Verify error message mentions file not found
       - Verify error suggests fallback files to try

    3. Create test "should reject invalid provider ID format":
       - Create config with provider ID containing invalid characters
       - Call initializeConfig() with this config
       - Verify validation error mentions invalid provider ID
       - Verify error explains valid ID format

    These tests ensure the config system provides helpful error messages when configuration is invalid.
  </action>
  <verify>npm test -- src/config/integration.test.ts</verify>
  <done>
    - Test validates invalid config produces clear error
    - Test validates missing config file provides helpful error
    - Test validates invalid provider ID is caught and explained
    - Error messages are actionable and guide user to fix config
    - All validation error tests pass
  </done>
  <commit_message>test(01-10): add config validation error tests

- Test invalid config produces descriptive error
- Test missing config file provides helpful error message
- Test invalid provider ID caught and explained
- Verify error messages guide user to fix configuration
</commit_message>
</task>

<task type="checkpoint:human-verify" priority="high" gate="blocking">
  <name>Manual verification of config system</name>
  <files>config/providers.yaml</files>
  <what-built>
    Complete config system with:
    - Config loading from YAML files
    - Environment variable overrides
    - Schema validation with clear error messages
    - Provider filtering based on API keys
    - End-to-end integration tests

    Old hardcoded provider system removed.
    Application is 100% config-driven.
  </what-built>
  <how-to-verify>
    1. Set at least one provider API key:
       export PROVIDER_MISTRAL_API_KEY="your_key_here"

    2. Start the application:
       npm start

    3. Verify startup logs show:
       - "Configuration loaded:"
       - "Providers: 1 (mistral)" (or however many you set)
       - "Server: localhost:8402"

    4. Test config file changes:
       - Edit config/providers.yaml to change server.port
       - Restart application
       - Verify new port is used

    5. Test environment overrides:
       - Set PROVIDER_GROQ_API_KEY
       - Set PROVIDER_GROQ_ENABLED=true
       - Restart application
       - Verify both mistral and groq appear in startup logs

    6. Test validation:
       - Corrupt config/providers.yaml (add invalid YAML)
       - Try to start application
       - Verify clear error message explaining what's wrong
  </how-to-verify>
  <resume-signal>Type "approved" if config system works as expected, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
After completion, verify:

1. **Integration tests pass**: `npm test -- src/config/integration.test.ts` shows 0 failures
2. **End-to-end test validates real config**: Test loads config/providers.yaml
3. **All providers available**: Test confirms all 9 providers from config are loadable
4. **ENV overrides work**: Tests confirm PROVIDER_{ID}_API_KEY pattern works
5. **Error messages helpful**: Validation errors provide clear guidance

Run full test suite: `npm test`

Manual verification checklist:
- [ ] Server starts with config from config/providers.yaml
- [ ] Startup logs show loaded providers correctly
- [ ] Changing config file affects runtime behavior
- [ ] Environment variables override config file values
- [ ] Invalid config produces helpful error messages
</verification>

<success_criteria>
- [ ] End-to-end integration test validates complete config flow
- [ ] Real config/providers.yaml loads successfully in tests
- [ ] All 9 providers are available at runtime when API keys set
- [ ] Environment variable overrides work correctly
- [ ] Config validation errors are clear and actionable
- [ ] Manual verification confirms config system works in production
- [ ] 5/5 must-haves from VERIFICATION.md now achieved
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-configuration-implementation/01-10-SUMMARY.md`

Then create `.planning/phases/01-core-configuration-implementation/01-GAP-CLOSURE-SUMMARY.md` documenting how all gaps were closed.
</output>
