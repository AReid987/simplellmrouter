---
phase: 01-core-configuration-implementation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/index.test.ts
  - src/config/loader.ts
  - src/config/index.ts
autonomous: true
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Config file loading works in test environment"
    - "Tests validate actual config/providers.yaml can be loaded"
    - "All 12 failing tests now pass"
  artifacts:
    - path: "src/config/index.test.ts"
      provides: "Unit tests for config module"
      min_lines: 339
    - path: "src/config/loader.ts"
      provides: "Config file loading with test support"
      min_lines: 155
    - path: "src/config/index.ts"
      provides: "Config initialization and access"
      min_lines: 207
  key_links:
    - from: "src/config/index.test.ts"
      to: "src/config/loader.ts"
      via: "jest mock for loadConfigFile"
      pattern: "jest.mock.*loader"
    - from: "src/config/index.test.ts"
      to: "config/providers.yaml"
      via: "loadConfigFile in tests"
      pattern: "loadConfigFile.*providers"
    - from: "src/config/index.ts"
      to: "src/config/loader.ts"
      via: "import and call loadConfigFile"
      pattern: "import.*loadConfigFile"
---

<objective>
Fix failing configuration tests by ensuring config files can be loaded in test environment.

Purpose: The current test failures stem from config files not being loaded properly in the test environment. The mocks are not correctly simulating the actual config loading behavior, causing "providers undefined" errors. This plan fixes the test setup and mock implementations to properly validate the config system.

Output: All 12 failing tests in src/config/index.test.ts now pass, validating that the configuration system works correctly.
</objective>

<execution_context>
@/Users/antonioreid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonioreid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-configuration-implementation/01-core-configuration-implementation-VERIFICATION.md
@.planning/phases/01-core-configuration-implementation/01-07-SUMMARY.md
@src/config/index.test.ts
@src/config/loader.ts
@src/config/index.ts
@config/providers.yaml
</context>

<tasks>

<task type="auto" priority="high" estimate="30m">
  <name>Fix test mock implementations for config loading</name>
  <files>src/config/index.test.ts</files>
  <action>
    Update the test mock implementations in src/config/index.test.ts to properly simulate config loading:

    1. Fix the beforeEach mock setup (lines 44-69):
       - Ensure loadConfigFile returns a valid config structure with providers
       - Ensure applyEnvMocks properly merges API keys into providerConfig
       - Ensure validateConfigOrThrow returns config with providers array

    2. Add proper provider structure to mocks:
       ```typescript
       providers: {
         mistral: {
           id: 'mistral',
           name: 'Mistral',
           baseUrl: 'https://api.mistral.ai/v1',
           enabled: true,
           models: [...] // Complete model definitions
         }
       },
       providerConfig: {
         mistral: { apiKey: 'test-key', enabled: true }
       }
       ```

    3. Fix the schema validation tests (lines 78-194) to use actual provider data from config/providers.yaml

    Do NOT change the test structure or test names - only fix the mock implementations to return valid config structures.
  </action>
  <verify>npm test -- src/config/index.test.ts</verify>
  <done>
    - All schema validation tests pass
    - getConfig() tests pass with proper initialization
    - getEnabledProviders() tests return providers with API keys
    - isConfigInitialized() tests pass
    - resetConfig() tests allow re-initialization
    - Error handling tests pass
  </done>
  <commit_message>test(01-08): fix config test mock implementations

- Fix beforeEach mock setup to return valid config structure
- Ensure providers are properly defined in mock data
- Add proper providerConfig with API keys to mocks
- All 12 failing tests now pass
</commit_message>
</task>

<task type="auto" priority="high" estimate="20m">
  <name>Add config loading test for actual config file</name>
  <files>src/config/integration.test.ts</files>
  <action>
    Add a new test to integration.test.ts that validates loading the actual config/providers.yaml file:

    1. Add new test case "should load actual config/providers.yaml file":
       - Load the real config file using loadConfigFile('providers.yaml')
       - Validate that it loads without errors
       - Verify it contains expected providers (mistral, groq, gemini, etc.)
       - Validate schema using AppConfigSchema.safeParse()

    2. This test should:
       - Use the actual loader (not mocked)
       - Set PROVIDER_MISTRAL_API_KEY environment variable
       - Validate real provider definitions from config/providers.yaml

    Place this test in a new describe block called "Real Config File Loading" to distinguish from mocked tests.
  </action>
  <verify>npm test -- src/config/integration.test.ts</verify>
  <done>
    - New test validates loading actual config/providers.yaml
    - Test verifies all 9 providers are defined
    - Test confirms schema validation passes for real config
    - Integration test suite passes
  </done>
  <commit_message>test(01-08): add real config file loading test

- Add test for loading actual config/providers.yaml
- Validate all 9 providers are present in real config
- Confirm schema validation passes for production config
- Integration test validates end-to-end config loading
</commit_message>
</task>

<task type="auto" priority="medium" estimate="15m">
  <name>Improve test error messages and debugging</name>
  <files>src/config/index.test.ts</files>
  <action>
    Improve test error messages to make debugging easier:

    1. Add descriptive error messages to test assertions:
       - Replace generic expect().toBe() with custom error messages
       - Add context about what was expected vs what was received

    2. Add debug logging to failing tests:
       - Log config structure when tests fail
       - Log provider count and IDs
       - Log API key presence (without revealing actual keys)

    3. Add test helper function for validating provider structure:
       ```typescript
       function validateProvider(provider: any) {
         expect(provider.id).toBeDefined();
         expect(provider.name).toBeDefined();
         expect(provider.baseUrl).toBeDefined();
         expect(provider.models).toBeInstanceOf(Array);
         expect(provider.models.length).toBeGreaterThan(0);
       }
       ```

    Do NOT change test logic - only improve observability and error messages.
  </action>
  <verify>npm test -- src/config/index.test.ts</verify>
  <done>
    - All tests have descriptive error messages
    - Failed tests provide useful debugging information
    - Test helper functions reduce code duplication
    - Test suite is more maintainable
  </done>
  <commit_message>test(01-08): improve test error messages and debugging

- Add descriptive error messages to all assertions
- Add debug logging for failing tests
- Add validateProvider helper function
- Tests are easier to debug and maintain
</commit_message>
</task>

</tasks>

<verification>
After completion, verify:

1. **All tests pass**: `npm test -- src/config/index.test.ts` shows 0 failures
2. **Integration tests pass**: `npm test -- src/config/integration.test.ts` shows 0 failures
3. **Real config loads**: New test successfully loads and validates config/providers.yaml
4. **No regressions**: Existing tests still pass
5. **Error messages useful**: Failed tests (if any) provide clear diagnostic information

Run full test suite: `npm test`
</verification>

<success_criteria>
- [ ] All 12 previously failing tests now pass
- [ ] New integration test validates actual config/providers.yaml
- [ ] Test error messages provide useful debugging information
- [ ] Config loading works in test environment
- [ ] No test mocks are brittle or implementation-dependent
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-configuration-implementation/01-08-SUMMARY.md`
</output>
