---
phase: 01-core-configuration-implementation
plan: 09
type: execute
wave: 2
depends_on: ["01-08"]
files_modified:
  - src/providers.ts
  - src/server.ts
  - src/server.test.ts
autonomous: true
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Application is fully driven by external configuration"
    - "No hardcoded provider definitions exist in codebase"
    - "Config file changes affect runtime behavior"
  artifacts:
    - path: "src/providers.ts"
      provides: "Utility functions only, no hardcoded definitions"
      max_lines: 150
    - path: "src/server.ts"
      provides: "Server using config-driven provider loading"
      contains: "getConfig(), getEnabledProviders()"
  key_links:
    - from: "src/server.ts"
      to: "src/config/index.ts"
      via: "import getConfig, getEnabledProviders"
      pattern: "import.*getConfig.*from.*config"
    - from: "src/cli.ts"
      to: "src/config/index.ts"
      via: "import initializeConfig"
      pattern: "import.*initializeConfig.*from.*config"
    - from: "application"
      to: "config/providers.yaml"
      via: "loadConfigFile at startup"
      pattern: "initializeConfig.*loadConfigFile"
---

<objective>
Remove hardcoded PROVIDER_DEFINITIONS and complete migration to config-driven provider system.

Purpose: The old hardcoded PROVIDER_DEFINITIONS in src/providers.ts (lines 51-298) creates a dual system where configuration exists but the application may still use hardcoded values. This plan removes the hardcoded definitions entirely, making the application fully driven by external configuration files.

Output: Application is 100% config-driven with no hardcoded provider definitions. Changes to config/providers.yaml immediately affect runtime behavior.
</objective>

<execution_context>
@/Users/antonioreid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonioreid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-configuration-implementation/01-core-configuration-implementation-VERIFICATION.md
@.planning/phases/01-core-configuration-implementation/01-08-SUMMARY.md
@src/providers.ts
@src/config/index.ts
@config/providers.yaml
</context>

<tasks>

<task type="auto" priority="high" estimate="30m">
  <name>Remove hardcoded PROVIDER_DEFINITIONS from providers.ts</name>
  <files>src/providers.ts</files>
  <action>
    Remove the hardcoded PROVIDER_DEFINITIONS constant (lines 51-298) from src/providers.ts:

    1. Delete lines 51-298 (PROVIDER_DEFINITIONS constant)
    2. Remove functions that depend on PROVIDER_DEFINITIONS:
       - Remove buildProvider() function (lines 320-337)
       - Remove loadAllProviders() function (lines 342-364)

    3. Keep ONLY these utility functions that are still useful:
       - Type definitions (lines 8-39): QuotaConfig, ModelConfig, Provider, ProviderConfig
       - loadProviderConfig() function (lines 303-315) - may still be useful for ENV-only mode
       - getModel() function (lines 369-381) - utility for finding models
       - getAllModels() function (lines 386-402) - utility for listing models
       - validateProviders() function (lines 407-427) - utility for validation

    4. Update getModel() and getAllModels() to work with config-driven providers:
       - They already accept Provider[] as parameter, so they should work unchanged
       - Just verify they don't reference PROVIDER_DEFINITIONS

    5. Add deprecation notice to remaining functions:
       ```typescript
       /**
        * @deprecated Use getConfig() and getEnabledProviders() from config module instead
        */
       ```

    The file should shrink from 428 lines to approximately 100-150 lines containing only types and utility functions.
  </action>
  <verify>
    grep -r "PROVIDER_DEFINITIONS" src/ && echo "FAIL: References still exist" || echo "PASS: Removed all references"
    wc -l src/providers.ts | awk '{if ($1 > 150) print "FAIL: File too large"; else print "PASS: File within expected size"}'
  </verify>
  <done>
    - PROVIDER_DEFINITIONS constant completely removed
    - buildProvider() function removed
    - loadAllProviders() function removed
    - File reduced to ~100-150 lines
    - Only types and utility functions remain
    - No hardcoded provider data exists in codebase
  </done>
  <commit_message>refactor(01-09): remove hardcoded provider definitions

- Delete PROVIDER_DEFINITIONS constant (248 lines removed)
- Remove buildProvider() and loadAllProviders() functions
- Keep only types and utility functions
- File reduced from 428 to ~150 lines
- Application now fully config-driven
</commit_message>
</task>

<task type="auto" priority="high" estimate="20m">
  <name>Update server imports to use config module exclusively</name>
  <files>src/server.ts</files>
  <action>
    Ensure server.ts uses config module for all provider access:

    1. Verify current imports (lines 12):
       - Already imports: getConfig, getEnabledProviders from './config/index.js'
       - Already imports: getModel, type Provider from './providers.js'

    2. Verify server startup (line 267):
       - Already calls: getConfig() and getEnabledProviders()
       - Ensure no calls to loadAllProviders() from providers.ts

    3. Search for any remaining references to old provider system:
       - Remove any imports or calls to loadAllProviders()
       - Replace with getEnabledProviders() if found

    4. Update error messages (lines 271-273) if they reference old system:
       - Current message: "Add API keys via environment variables: PROVIDER_MISTRAL_API_KEY..."
       - This is still correct, keep as-is

    The server should already be using the config system, so this task is primarily verification and cleanup.
  </action>
  <verify>
    grep "loadAllProviders" src/server.ts && echo "FAIL: Old system still referenced" || echo "PASS: Using config system"
    grep "getEnabledProviders" src/server.ts | grep -v "^[[:space:]]*\/\/" && echo "PASS: Using config system"
  </verify>
  <done>
    - No references to loadAllProviders()
    - All provider access via getConfig() and getEnabledProviders()
    - Server fully config-driven
    - Error messages reference correct environment variables
  </done>
  <commit_message>refactor(01-09): verify server uses config module exclusively

- Verify all provider access uses getConfig/getEnabledProviders
- Remove any remaining references to old provider system
- Server fully config-driven
</commit_message>
</task>

<task type="auto" priority="high" estimate="30m">
  <name>Update server tests to use config system</name>
  <files>src/server.test.ts</files>
  <action>
    Update server.test.ts to mock the config system instead of the old provider system:

    1. Update imports (lines 6, 8):
       - Remove: import { loadAllProviders, getModel } from './providers'
       - Add: import { getEnabledProviders } from './config'
       - Keep: import { initializeConfig, resetConfig } from './config'

    2. Update mocks (lines 26-29, 98-124):
       - Remove: jest.mock('./providers', { loadAllProviders, getModel })
       - Add: jest.mock('./config', () => ({
           initializeConfig: jest.fn(),
           resetConfig: jest.fn(),
           getConfig: jest.fn(),
           getEnabledProviders: jest.fn(),
         }))

    3. Update beforeAll setup (lines 50-142):
       - Mock initializeConfig() to resolve with valid config
       - Mock getEnabledProviders() to return test providers
       - Remove loadAllProviders() mock
       - Update getModel() to be a local helper or mock from providers module

    4. Update test "should throw error if no providers configured" (lines 263-270):
       - Mock getEnabledProviders() to return empty array
       - Remove loadAllProviders() mock

    5. Update test "should log rate limit events" (lines 272-349):
       - Mock getEnabledProviders() to return test providers with rate-limited model
       - Ensure getModel() works with the mocked providers

    The tests should mock the config system, not the old providers.ts module.
  </action>
  <verify>npm test -- src/server.test.ts</verify>
  <done>
    - All server tests mock config system, not old providers
    - Tests validate getEnabledProviders() behavior
    - No references to loadAllProviders() in tests
    - All tests pass
  </done>
  <commit_message>test(01-09): update server tests to use config system

- Mock config module instead of providers module
- Remove loadAllProviders() mocks from tests
- Tests validate getEnabledProviders() behavior
- All server tests pass with new config system
</commit_message>
</task>

<task type="auto" priority="medium" estimate="15m">
  <name>Add deprecation notice to providers.ts utilities</name>
  <files>src/providers.ts</files>
  <action>
    Add deprecation notices to remaining functions in providers.ts:

    1. Add JSDoc deprecation notice to file header:
       ```typescript
       /**
        * Provider Utilities
        *
        * @deprecated Most functionality has moved to src/config/index.ts
        * Use getConfig() and getEnabledProviders() for provider access.
        * These utilities are kept for backward compatibility.
        */
       ```

    2. Add deprecation notices to each exported function:
       - loadProviderConfig(): "Use PROVIDER_{ID}_API_KEY environment variables instead"
       - getModel(): "Use config.getModel() or iterate through getEnabledProviders()"
       - getAllModels(): "Use getEnabledProviders() and map models"
       - validateProviders(): "Config validation happens automatically in initializeConfig()"

    3. Do NOT deprecate type definitions (QuotaConfig, ModelConfig, Provider, ProviderConfig) as they are still used

    This provides clear guidance to future developers about which module to use.
  </action>
  <verify>grep -c "@deprecated" src/providers.ts | awk '{if ($1 >= 5) print "PASS: Deprecation notices added"; else print "FAIL: Missing deprecation notices"}'</verify>
  <done>
    - File header explains deprecation
    - All exported functions have @deprecated notices
    - Type definitions remain undeprecated
    - Clear migration path documented in comments
  </done>
  <commit_message>docs(01-09): add deprecation notices to providers.ts

- Add file-level deprecation notice
- Document migration path to config module
- Add @deprecated to all exported functions
- Keep type definitions undeprecated
</commit_message>
</task>

</tasks>

<verification>
After completion, verify:

1. **No hardcoded providers**: `grep -r "PROVIDER_DEFINITIONS" src/` returns nothing
2. **Config system used**: `grep "getEnabledProviders" src/*.ts` shows usage in server.ts
3. **Tests pass**: `npm test` shows 0 failures
4. **File size reduced**: `wc -l src/providers.ts` shows ~150 lines (down from 428)
5. **Deprecation documented**: `grep "@deprecated" src/providers.ts` shows notices

Manual verification:
- Start server with PROVIDER_MISTRAL_API_KEY set
- Verify providers are loaded from config/providers.yaml
- Modify config file and verify changes take effect on restart
</verification>

<success_criteria>
- [ ] PROVIDER_DEFINITIONS completely removed from codebase
- [ ] Application fully driven by config/providers.yaml
- [ ] Config file changes affect runtime behavior
- [ ] All tests pass with new config system
- [ ] Deprecation notices guide future developers
- [ ] No dual systems remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-configuration-implementation/01-09-SUMMARY.md`
</output>
